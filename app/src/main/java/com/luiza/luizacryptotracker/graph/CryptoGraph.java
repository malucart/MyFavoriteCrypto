package com.luiza.luizacryptotracker.graph;

import android.content.Context;

import androidx.appcompat.app.AppCompatActivity;

import com.jjoe64.graphview.GraphView;
import com.jjoe64.graphview.series.DataPoint;
import com.jjoe64.graphview.series.LineGraphSeries;
import com.luiza.luizacryptotracker.R;
import com.luiza.luizacryptotracker.model.CryptoModel;

import java.util.ArrayList;
/*
public class CryptoGraph extends AppCompatActivity  {

    public static final String TAG = "CryptoGraph";

    public void plotCryptoGraph(ArrayList<CryptoModel> cryptoModels) {

        GraphView graph = (GraphView) findViewById(R.id.gvGraph);
        LineGraphSeries<DataPoint> series = new LineGraphSeries<DataPoint>(new DataPoint[] {
                new DataPoint(0, 1),
                new DataPoint(1, 5),
                new DataPoint(2, 3),
                new DataPoint(3, 2),
                new DataPoint(4, 6)
        });
        graph.addSeries(series);

    }
}
 */
